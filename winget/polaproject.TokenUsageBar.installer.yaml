# Installer manifest for winget. Submit the trio in this folder to
# https://github.com/microsoft/winget-pkgs (path: manifests/p/polaproject/TokenUsageBar/1.0.0/)
# IMPORTANT: replace InstallerSha256 with the SHA256 of the EXACT exe attached to
# the GitHub release (the CI workflow prints it; or run:
#   (Get-FileHash dist\TokenUsageBar.exe -Algorithm SHA256).Hash )
PackageIdentifier: polaproject.TokenUsageBar
PackageVersion: 1.0.0
MinimumOSVersion: 10.0.0.0
InstallerType: portable
Commands:
  - tokenusagebar
ReleaseDate: 2026-06-01
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/polaproject/token-usage-bar/releases/download/v1.0.0/TokenUsageBar.exe
    InstallerSha256: REPLACE_WITH_SHA256_OF_RELEASED_EXE
ManifestType: installer
ManifestVersion: 1.6.0
